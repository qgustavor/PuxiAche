# Required for production builds/deploys — the game won't start without this.
# `bun run dev` doesn't need this file at all: it runs against the Firebase Local
# Emulator Suite (see firebase.json) instead of a real project.

VITE_FIREBASE_DATABASE_URL=https://puxiache-default-rtdb.firebaseio.com

